import { NeutralColors } from '@fluentui/react';

export const timePickerHeaderStyles = {
    root: { borderBottom: `1px solid ${NeutralColors.gray30}`, marginBlockEnd: '16px', padding: '24px 32px' },
};

export const timeSlotsListContainerStyles = { root: { overflow: 'auto' } };
